class Solution {
typedef ListNode ln;
public:
    ln reverse(ln head){
        ln prev = nullptr;
        ln curr = head;
        while(cu
            r!=nullptr){
            ln next = curr->next;
            curr->next = prev;
            prev = 
        }
    }
    bool isPalindrome(ListNode* head) {
        ln slow = head;
        ln fast =  head;

    }
};